{
  "name": "usbackendtest",
  "version": "1.0.0",
  "description": "Test for developing backend",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf build && cd ../us2.0 && npm run build && cd .. && cp -R us2.0/build usbackend/",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git checkout main && git push",
    "merge": "git checkout main && git merge test && git push && git checkout test",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rasmus Ruonakoski",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.18.1",
    "mongoose": "^8.9.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "typescript": "^5.7.3"
  }
}
